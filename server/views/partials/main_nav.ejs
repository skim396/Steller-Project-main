<!--Akshit Narang, #301177669, COMP 229, Section 008-->

<nav class="navbar navbar-expand-lg navbar-light bg-azure">
  <div class="container-fluid">
    <a href="/">
      
      
      <img class="nav_img" src="/Assets/images/Stellar-Logo.png" alt="Logo" style="width:100px;" /></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      
      <ul class="nav navbar-nav navbar-right">
          
          <!--Navigation bar links-->
          <li class="nav-item">
          <% if (title == "Home") { %>
            <a class="nav-link active" href="/"><i class="fas fa-lg fa-home"></i> Home</a>
          <% } else { %>
            <a class="nav-link" href="/"><i class="fas fa-lg fa-home"></i> Home</a>
          <% } %>
          </li>

          <li class="nav-item">
          <% if(title == "Survey List") { %>
            <a class="nav-link active" href="/survey-list"><i class="fas fa-lg fa-info"></i> Survey</a>
          <% } else { %>
            <a class="nav-link" href="/survey-list"><i class="fas fa-lg fa-info"></i> Survey</a>
          <% } %>
          </li>

          
          <li class="nav-item">
          <% if(title == "Login") { %>
            <% if(typeof displayName == 'string' && displayName) { %>
              <a href="/logout" class="nav-link active"><i class="fas fa-lg fa-sign-out-alt"></i>Logout</a></li>  
              <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>   
            <% } else { %>
              <li class="nav-item"><a href="/login" class="nav-link active">
              <i class="fas fa-lg fa-sign-in-alt"></i>Login</a></li>
            <% } %>
          <% } else { %>
            <% if(typeof displayName == 'string' && displayName) { %>
              <a href="/logout" class="nav-link"><i class="fas fa-lg fa-sign-out-alt"></i>Logout</a></li>  
              <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>   
            <% } else { %>
              <li class="nav-item"><a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i>Login</a></li>
            <% } %>
          <% } %>
          </li>


        </ul>
      </div>
    </div>
  </nav>